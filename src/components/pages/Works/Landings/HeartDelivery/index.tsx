import Detail from "@components/templates/Detail";
import { landings } from "../../list";

function HeartDelivery () {
    return (
        <Detail descript={landings.heartDelivery} />
    )
}

export default HeartDelivery